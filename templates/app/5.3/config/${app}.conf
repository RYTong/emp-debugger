%% ${app} configuration parameters.

%%----------------------------------------------------------------------
%% we load these parameters in ${app}_bootstrap and store them in ets.
%%----------------------------------------------------------------------

%% The file path of import channels and collections.
{menu, "config/channel.conf"}.


%% ~@doc 如果你需要使用push功能，请打开下面的注释~
%% {push, [
%%     {${app}_test, [
%%         {ios, [
%%             {certfile, "public/key/bankbh_cert.pem"},
%%             {keyfile, "public/key/bankbh_key.pem"}
%%             %% {pool_size, 10},
%%         ]}
%%     ]},
%%     {${app}, [
%%         {ios, [
%%             {certfile, "public/key/bankbh_cert.pem"},
%%             {keyfile, "public/key/bankbh_key.pem"},
%%             {env, development}
%%         ]},
%%         {android, [{xmpp_host, "http://127.0.1:5280/rest"}]}
%%     ]}
%% ]}.